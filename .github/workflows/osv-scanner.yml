name: OSV-Scanner

# Default branch is "develop"
on:
  pull_request:
  push:
    branches: [ develop ]
  merge_group:

# Declaring default permissions as read only.
permissions: read-all

jobs:
  scan-pr:
    uses: "google/osv-scanner/.github/workflows/osv-scanner-reusable-pr.yml@c50977954ca3d2cc4cf37c52d2dec3e75a97b7a5"
