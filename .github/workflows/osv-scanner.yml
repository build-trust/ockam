name: OSV-Scanner

# Default branch is "develop"
on:
  pull_request:
  push:
    branches: [ develop ]
  merge_group:

# Declaring default permissions as read only.
permissions: read-all

jobs:
  scan-pr:
    uses: "google/osv-scanner/.github/workflows/osv-scanner-reusable-pr.yml@46aee59befed6edb5fc737ef35b5febf987cffa9"
