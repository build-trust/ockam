[package]
name = "ockam_get_started"
version = "0.1.0"
authors = ["Ockam Developers"]
edition = "2018"

[dependencies]
ockam = { path = "../../../implementations/rust/ockam/ockam", features = ["ockam_transport_tcp", "ockam_vault"] }
ockam_transport_websocket = { path = "../../../implementations/rust/ockam/ockam_transport_websocket" }

[dev-dependencies]
rand = { version = "0.8.4", features = ["std_rng"] }
