
rootProject.name = 'c'

boolean inComposite = gradle.parent != null
if (!inComposite) {
  includeBuild '../../tools/gradle/plugins/host'
  includeBuild '../../tools/gradle/plugins/builder'
  includeBuild 'test/vault/atecc508a'
}
