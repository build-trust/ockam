[package]
edition = "2018"
name = "ockam_nodejs_native"
version = "0.0.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "1"
napi-derive = "1"

[build-dependencies]
napi-build = "1"

[profile.release]
lto = true

[workspace]
