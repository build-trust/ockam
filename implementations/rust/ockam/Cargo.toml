[package]
name = "ockam"
version = "0.1.0"
authors = ["Ockam Developers"]
edition = "2018"

[features]
alloc = []
default = ["alloc"]
std = ["alloc"]

[dependencies]
cfg-if = "1.0"
downcast = "0.10"
heapless = { version = "0.5", optional = true }

