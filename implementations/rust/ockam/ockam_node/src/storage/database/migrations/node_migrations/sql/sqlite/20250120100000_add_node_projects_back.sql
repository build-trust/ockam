-- This table is temporarily added back to make it consistent with the current code.
CREATE TABLE node_project
(
    node_name    TEXT PRIMARY KEY, -- Node name
    project_name TEXT NOT NULL     -- Project name
);
