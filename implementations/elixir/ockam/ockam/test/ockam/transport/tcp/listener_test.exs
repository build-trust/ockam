defmodule Ockam.Transport.TCP.Listener.Tests do
  use ExUnit.Case, async: true
  doctest Ockam.Transport.TCP.Listener
  alias Ockam.Transport.TCP.Listener
  alias Ockam.Transport.TCP.Listener.Handler

  describe "Ockam.Transport.TCP.Listener" do
    test "placeholder test" do
      assert true
    end
  end

  describe "Ockam.Transport.TCP.Listener.Handler" do
    test "placehoder test" do
      assert true
    end
  end
end
